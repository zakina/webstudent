
{# src/webStudent/EtudiantBundle/Resources/views/Etudiant/index.html.twig #}
<!DOCTYPE html>
<html>
 <head>
 <title>Webstudent</title>
 </head>
 <body>
 <h2>BIENVENUE SUR WEBSTUDENT, LE SITE DES ETUDIANTS DU LYCEE JEAN ROSTAND</h2>
 <h3>PAGE D'ACCUEIL : affichage des périodes de stage à venir</h3>
 
 <p>
Bienvenu
 Date d'aujourd'hui : {{ "now"|date('d/m/Y') }}
 Annee d'aujourd'hui : {{ "now"|date('Y') }}
 
 {%set annee = "now"|date('Y') %}
 {%set dateCompar = annee+1 ~'-09-01' %}
 date calculee = {{dateCompar}}
 
 {# LA COMPARAISON SUR LES DATES NE FONCTIONNE PAS #}
 {# LA COMPARAISON SUR LES DATES NE FONCTIONNE PAS #}
 {# LA COMPARAISON SUR LES DATES NE FONCTIONNE PAS #}
 {# LA COMPARAISON SUR LES DATES NE FONCTIONNE PAS #}
 
 <table>
 <tr>
			<td>Section</td>
			<td>Niveau</td>
			<td>Date de debut</td>
			<td>Date de fin</td>
			<td>Nb de semaines</td>
		</tr>
	{% for periode in listePeriodeStage %}
		{% if periode.DateDebut|date('d/m/Y') > dateCompar|date('d/m/Y')%}
			<tr>
			<td>{{periode.section.code }}</td>
			<td>{{periode.niveau.id }}</td>
			<td>{{ periode.DateDebut|date('d/m/Y') }}</td>
			<td>{{ periode.DateFin|date('d/m/Y') }}</td>
			<td>{{ periode.nbSemaines }}</td>
			</tr>
		{% endif %}
	{% endfor %}

</table>
 
 
 

 </p>
 </body>
</html>
